{
  "name": "timestamp-microservice",
  "version": "0.1.0",
  "description": "FCC project to build a microservice that converts dates and unix timestamps.",
  "main": "build/server.js",
  "scripts": {
    "test": "npm test",
    "start": "node build/server.js",
    "build": "mkdir -p build && babel src/server.js -o build/server.js && cp -R src/views build",
    "dev": "babel-node src/server.js"
  },
  "keywords": [
    "freecodecamp",
    "microservice",
    "api"
  ],
  "author": "Jonathan Willis",
  "license": "GPL-2.0",
  "dependencies": {
    "consolidate": "^0.13.1",
    "express": "^4.13.3",
    "nunjucks": "^2.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.4.0",
    "babel-core": "^6.4.0",
    "babel-polyfill": "^6.3.14",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.4.3",
    "chai": "^3.4.1",
    "mocha": "^1.3.4"
  }
}
